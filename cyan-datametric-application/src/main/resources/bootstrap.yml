# 基础应用配置
spring:
  profiles:
    active: dev
  application:
    name: cyan-datametric
  main:
    web-application-type: servlet

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
